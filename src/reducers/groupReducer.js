export default function groupReducer(state = {groups: [], contacts: []}, action) {
    // switch (action.type)
    //     case "ADD_GROUP":
    //         return state
    //     default:
    return state
}